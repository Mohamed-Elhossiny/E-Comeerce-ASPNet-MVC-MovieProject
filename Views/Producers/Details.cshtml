@model ProducersDetailsViewModel
@{
	ViewData["Title"] = "Details";
}
<div class="container">
	<div class="row">
		<div class="col-md-8 offset-2">
			<h1 class="text-center"> @Model.FullName </h1>
			<div class="row">
				<div class="col-md-8 offset-2">
					<div asp-validation-summary="ModelOnly" class="text-danger"></div>
					<div class="form-group text-center">
						<img src="/images/Producers/@Model.PictureURl" class="rounded-circle" style="max-width:200px" />
					</div>
					<div class="form-group">
						<label asp-for="PictureURl" class="control-label"></label>
						<input asp-for="PictureURl" class="form-control" readonly />
						<span asp-validation-for="PictureURl" class="text-danger"></span>
					</div>
					<div class="form-group">
						<label asp-for="FullName" class="control-label"></label>
						<input asp-for="FullName" class="form-control" readonly />
						<span asp-validation-for="FullName" class="text-danger"></span>
					</div>
					<div class="form-group">
						<label asp-for="Bio" class="control-label"></label>
						<input asp-for="Bio" class="form-control" readonly />
						<span asp-validation-for="Bio" class="text-danger"></span>
					</div>
					<div class="form-group mt-3">
						@if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
						{
							<a asp-action="Edit" asp-controller="Producers" asp-route-id="@Model.Id" class="btn btn-outline-success float-end"><i class="bi bi-pencil-square"></i> Edit</a>
						}
						<a asp-action="Index" asp-controller="Producers" class="btn btn-outline-secondary">Show All</a>
					</div>
				</div>
			</div>
		</div>
	</div>

</div>

@section Scripts {
	@{
		await Html.RenderPartialAsync("_ValidationScriptsPartial");
	}
}